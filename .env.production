# lux.eternum.lat corre sobre los mocks de MSW porque todavia no hay backend:
# sin esto el login queda muerto y /graphql responde 405 desde nginx.
# Cuando exista la API real, poner false y borrar el archivo estatico
# public/mockServiceWorker.js.
VITE_ENABLE_MOCKS=true
